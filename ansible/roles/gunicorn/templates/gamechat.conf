description "gamechat"

start on (filesystem)
stop on runlevel [016]

respawn
setuid ubuntu
setgid ubuntu
chdir /home/ubuntu/game-chat/gamechat/gamechat
env DATABASE_URL={{ db_url }}
exec gunicorn -b 127.0.0.1:8000 -w 4 gamechat.wsgi --error-logfile /tmp/gunicorn-error.log
